import { AppModulesSportCompetitionSchemasSportCompetitionPaymentComplete } from "@/src/api/hyperionSchemas";

export const UserPayments = ({
  userPayments,
}: {
  userPayments: AppModulesSportCompetitionSchemasSportCompetitionPaymentComplete[];
}) => {
  return (
    <div className="">
      <h2>Paiements</h2>
    </div>
  );
};
