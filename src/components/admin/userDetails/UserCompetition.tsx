import {
  CompetitionUser,
  ParticipantComplete,
} from "@/src/api/hyperionSchemas";

export const UserCompetition = ({
  user,
  userParticipant,
}: {
  user: CompetitionUser;
  userParticipant: ParticipantComplete;
}) => {
  return (
    <div className="">
      <h2>Particpant</h2>
    </div>
  );
};
